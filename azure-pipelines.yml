# Docker
# Build a Docker image
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- main

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
    # - task: Docker@2
    #   displayName: Build an image
    #   inputs:
    #     command: build
    #     dockerfile: '$(Build.SourcesDirectory)/Dockerfile'
    #     tags: |
    #       $(tag)

    # - task: Docker@2
    #   displayName: Push the image
    #   inputs:
    #     containerRegistry: 'dockerhub-connection'
    #     repository: 'pdemeulenaer/mlflow-server'
    #     command: 'push'

    # - task: Docker@2
    #   inputs:
    #     containerRegistry: 'dockerhub-connection'
    #     repository: 'pdemeulenaer/mlflow-server'
    #     command: 'buildAndPush'
    #     Dockerfile: '$(Build.SourcesDirectory)/Dockerfile'



    - task: oc-cmd@2
      inputs:
        connectionType: 'OpenShift Connection Service'
        openshiftService: 'openshift_connect'
        cmd: 'oc login --token=sha256~gfJUtNZzeVd-3c12WGl75fE3ir4BTcTPh7uDDy9uXow --server=https://api.sandbox-m2.ll9k.p1.openshiftapps.com:6443 & oc get deploy'
        uselocalOc: true